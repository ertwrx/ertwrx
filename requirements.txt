blinker==1.9.0
click==8.1.8
# dbus-python==1.3.2   # system-level dependency, requires native libs
distro==1.9.0
Flask==3.1.0
iniconfig==2.1.0
itsdangerous==2.2.0
Jinja2==3.1.6
# libcomps==0.1.21      # RPM/Fedora-specific, not available via pip
# libdnf==0.74.0        # RPM/YUM bindings, not on PyPI
MarkupSafe==3.0.2
# mercurial==6.8.2      # not on PyPI or hard to install on Ubuntu
# nftables==0.1         # system-level, not installable via pip
packaging==25.0
pluggy==1.5.0
# PyGObject==3.48.2     # requires GTK/GObject system libs
pyparsing==3.1.2
pytest==8.3.5
python-dateutil==2.8.2
python-dotenv==1.1.0
# rpm==4.20.1           # RPM binding, not available on Ubuntu
six==1.16.0
# systemd-python==235   # needs libsystemd-dev and native bindings
Werkzeug==3.1.3
